## MacOS
- `.pkg` installer from site (automatic version replacement)
- `brew`

## Windows
- `.msi` installer from site (automatic version replacement)
- `choco`

## Linux
1. Unarchive gzipped TAR
2. Copy go directory to `/usr/local`
3. Add `/usr/local/go/bin` to `$PATH <br/> 
Note: Better to backup an old go binary before new one is installed